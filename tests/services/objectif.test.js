import { objectifService } from '../../lib/services';

describe('objectifService',()=>{
    let entities = [{userId:'1'}];
    describe('findByIdLevel',()=>{
        test('Should return objectifs for levels 1',async()=>{
            
        })
    })
})